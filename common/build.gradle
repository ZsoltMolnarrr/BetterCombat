architectury {
    common rootProject.enabled_platforms.split(',')
}

repositories {
    mavenCentral()
    maven {
        name = "RedlanceMinecraft"
        url = "https://repo.redlance.org/public"
    }
    maven { url 'https://maven.shedaniel.me/' }
    maven { url 'https://maven.fabricmc.net/' }
    maven { url 'https://jitpack.io' }
    maven { url 'https://maven.terraformersmc.com/' }
    maven {
        name = 'Modrinth'
        url = 'https://api.modrinth.com/maven'
        content {
            includeGroup 'maven.modrinth'
        }
    }
    exclusiveContent {
        forRepository {
            maven {
                url = "https://maven.ftb.dev/releases"
            }
        }
        filter {
            includeGroup"dev.latvian.mods"
            includeGroup"dev.ftb.mods"
        }
    }
}

dependencies {
    // We depend on Fabric Loader here to use the Fabric @Environment annotations,
    // which get remapped to the correct annotations on each platform.
    // Do NOT use other classes from Fabric Loader.
    implementation "net.fabricmc:fabric-loader:$project.fabric_loader_version"
    // compileOnly(annotationProcessor("io.github.llamalad7:mixinextras-common:${project.mixin_extras_version}"))

    api("me.shedaniel.cloth:cloth-config-fabric:${project.cloth_config_version}") {
        exclude(group: "net.fabricmc.fabric-api")
    }

    api("com.zigythebird.playeranim:PlayerAnimationLibCommon:${project.player_anim_version}")
    implementation("com.zigythebird.playeranim:PlayerAnimationLibCore:${project.player_anim_version}")
    implementation("maven.modrinth:tiny-config:${project.tiny_config_version}-fabric")

    compileOnly("maven.modrinth:first-person-model:${project.fpm_version}")

    compileOnly("dev.ftb.mods:ftb-teams:${project.ftb_teams_version}")
    compileOnly("dev.ftb.mods:ftb-library:${project.ftb_library_version}")

    // TODO: Restore compile-only compat dependencies once 26.1 builds are published
    // (pre-26.1 jars are intermediary-mapped, unusable without remapping)
    //compileOnly("maven.modrinth:spell-engine:${project.spell_engine_version}-fabric")
}